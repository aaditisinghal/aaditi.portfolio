import React from 'react';
import { motion } from 'framer-motion';
import { Badge } from '@/components/ui/badge';
import { ExternalLink, TrendingUp, Sparkles } from 'lucide-react';

export default function ProjectCard({ project, index }) {
  return (
    <motion.div
      layout
      initial={{ opacity: 0, y: 20 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true }}
      transition={{ duration: 0.5, delay: index * 0.1 }}
      whileHover={{ scale: 1.02, y: -5 }}
      className="group relative"
    >
      {/* Decorative corner accents */}
      <div className="absolute -top-2 -left-2 w-8 h-8 border-t-2 border-l-2 border-purple-400/20 rounded-tl-xl opacity-0 group-hover:opacity-100 transition-all duration-500 group-hover:w-12 group-hover:h-12"></div>
      <div className="absolute -top-2 -right-2 w-8 h-8 border-t-2 border-r-2 border-purple-400/20 rounded-tr-xl opacity-0 group-hover:opacity-100 transition-all duration-500 group-hover:w-12 group-hover:h-12"></div>
      <div className="absolute -bottom-2 -left-2 w-8 h-8 border-b-2 border-l-2 border-purple-400/20 rounded-bl-xl opacity-0 group-hover:opacity-100 transition-all duration-500 group-hover:w-12 group-hover:h-12"></div>
      <div className="absolute -bottom-2 -right-2 w-8 h-8 border-b-2 border-r-2 border-purple-400/20 rounded-br-xl opacity-0 group-hover:opacity-100 transition-all duration-500 group-hover:w-12 group-hover:h-12"></div>
      
      {/* Glow effect */}
      <div className="absolute inset-0 bg-gradient-to-r from-purple-600/0 via-purple-600/10 to-purple-600/0 rounded-3xl opacity-0 group-hover:opacity-100 transition-opacity duration-500 blur-xl"></div>
      
      <div className="relative bg-black/60 backdrop-blur-xl rounded-3xl overflow-hidden border border-purple-500/20 shadow-2xl hover:shadow-purple-500/40 transition-all duration-500 h-full hover:border-purple-500/40">
        {/* Project Image */}
        {project.image && (
          <div className="relative h-48 overflow-hidden">
            <img 
              src={project.image} 
              alt={project.title}
              className="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700"
            />
            {/* Gradient overlay */}
            <div className="absolute inset-0 bg-gradient-to-t from-black/90 via-black/50 to-transparent"></div>
            
            {/* Animated shimmer on hover */}
            <div className="absolute inset-0 bg-gradient-to-r from-transparent via-white/10 to-transparent translate-x-[-200%] group-hover:translate-x-[200%] transition-transform duration-1000"></div>
            
            {/* Badge overlay on image */}
            <div className="absolute top-4 left-4">
              <Badge className={`bg-gradient-to-r ${project.gradient} text-white border-0 px-4 py-1.5 text-sm font-semibold shadow-lg backdrop-blur-sm relative overflow-hidden`}>
                <span className="relative z-10 flex items-center gap-1">
                  <Sparkles className="w-3 h-3" />
                  {project.badge}
                </span>
                <span className="absolute inset-0 bg-white/20 translate-x-[-100%] group-hover:translate-x-[100%] transition-transform duration-700"></span>
              </Badge>
            </div>
            
            {/* Date overlay on image */}
            <div className="absolute top-4 right-4">
              <Badge className="bg-black/60 backdrop-blur-md text-purple-200 border border-purple-400/30 text-sm shadow-lg">
                {project.date}
              </Badge>
            </div>
            
            {/* Decorative corner dots */}
            <div className="absolute bottom-2 left-2 w-2 h-2 rounded-full bg-purple-400/50 animate-pulse"></div>
            <div className="absolute bottom-2 right-2 w-2 h-2 rounded-full bg-purple-400/50 animate-pulse animation-delay-500"></div>
          </div>
        )}
        
        {/* Content */}
        <div className="p-8 relative">
          {/* Badge and Date (only if no image) */}
          {!project.image && (
            <div className="flex justify-between items-start mb-4">
              <Badge className={`bg-gradient-to-r ${project.gradient} text-white border-0 px-4 py-1.5 text-sm font-semibold shadow-lg relative overflow-hidden`}>
                <span className="relative z-10">{project.badge}</span>
                <span className="absolute inset-0 bg-white/20 translate-x-[-100%] group-hover:translate-x-[100%] transition-transform duration-700"></span>
              </Badge>
              <p className="text-purple-300 text-sm">{project.date}</p>
            </div>
          )}

          {/* Title with elegant underline effect */}
          <div className="relative mb-4">
            <h3 className="text-2xl font-bold text-white mb-2 group-hover:text-purple-300 transition-colors relative z-10">
              {project.title}
            </h3>
            <div className="absolute bottom-0 left-0 h-0.5 w-0 bg-gradient-to-r from-purple-400 to-pink-400 group-hover:w-full transition-all duration-500"></div>
          </div>
          <p className="text-lg text-purple-200 mb-4 font-medium">{project.subtitle}</p>

          {/* Description */}
          <p className="text-purple-100/80 mb-6 leading-relaxed text-sm">
            {project.description}
          </p>

          {/* Metrics with enhanced styling */}
          <div className="grid grid-cols-3 gap-4 mb-6">
            {project.metrics.map((metric, i) => (
              <motion.div 
                key={i} 
                whileHover={{ scale: 1.05, y: -2 }}
                className="relative bg-purple-600/10 rounded-xl p-3 border border-purple-400/20 hover:bg-purple-600/20 transition-all duration-300 overflow-hidden group/metric"
              >
                {/* Animated background on hover */}
                <div className="absolute inset-0 bg-gradient-to-br from-purple-500/20 to-pink-500/20 translate-y-full group-hover/metric:translate-y-0 transition-transform duration-300"></div>
                
                <p className="relative z-10 text-purple-400 text-xs uppercase tracking-wider mb-1 font-semibold">
                  {metric.label}
                </p>
                <p className="relative z-10 text-white font-bold text-sm">
                  {metric.value}
                </p>
                
                {/* Corner accent */}
                <div className="absolute top-1 right-1 w-1 h-1 rounded-full bg-purple-400/50 opacity-0 group-hover/metric:opacity-100 transition-opacity"></div>
              </motion.div>
            ))}
          </div>

          {/* Tags */}
          <div className="flex flex-wrap gap-2 mb-4">
            {project.tags.map((tag, i) => (
              <Badge 
                key={tag} 
                className="bg-purple-600/20 text-purple-200 border border-purple-400/30 hover:bg-purple-600/30 hover:border-purple-400/50 transition-all duration-300 text-xs backdrop-blur-sm relative overflow-hidden group/tag"
              >
                <span className="relative z-10">{tag}</span>
                <span className="absolute inset-0 bg-gradient-to-r from-purple-500/0 via-purple-500/30 to-purple-500/0 translate-x-[-100%] group-hover/tag:translate-x-[100%] transition-transform duration-700"></span>
              </Badge>
            ))}
          </div>
        </div>

        {/* Hover Effect - Multi-layered */}
        <div className="absolute inset-0 bg-gradient-to-r from-purple-600/0 via-purple-600/5 to-purple-600/0 opacity-0 group-hover:opacity-100 transition-opacity duration-500 rounded-3xl pointer-events-none"></div>
        <div className="absolute inset-0 bg-gradient-to-br from-purple-600/0 via-pink-600/5 to-purple-600/0 opacity-0 group-hover:opacity-100 transition-opacity duration-700 rounded-3xl pointer-events-none"></div>
      </div>
      
      <style jsx>{`
        .animation-delay-500 {
          animation-delay: 0.5s;
        }
      `}</style>
    </motion.div>
  );
}
